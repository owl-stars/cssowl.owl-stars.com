%h2#cssowl-sprite-after-inline cssowl-sprite-after-inline <small>(x, y, width, height, src, xOffset, yOffset, valign)</small>

%p
  :markdown
    Adds an element with the `:after` pseudo selector and displays it *inline*.

= preserve do
  -code("sass") do
    :plain
      /*
       * @param $x x position of the element
       * @param $y y position of the element
       * @param $width element width
       * @param $height element height
       * @param $src background image source
       * @param $xOffset (optional, default=0) left margin of the element
       * @param $yOffset (optional, default=0) top margin of the element
       * @param $valign (optional, default=middle) *vertical-align* attribute property
       */
      @mixin cssowl-sprite-after-inline($x, $y, $width, $height, $src, $xOffset:0, $yOffset:0, $valign:middle)
        &:after
          content: ""
          margin-left: $xOffset
          +cssowl-sprite-display-inline($x, $y, $width, $height, $src, $yOffset, $valign)

.bs-docs-example

  .content
    .example-after-inline Quia sint quis nam et quia ducimus consequatur cumque.

  %ul.nav.nav-tabs
    %li.active
      = link_to 'Sass', '#example-after-inline-sass'
    %li
      = link_to 'Scss', '#example-after-inline-scss'
    %li
      = link_to 'Less', '#example-after-inline-less'
    %li
      = link_to 'Stylus', '#example-after-inline-stylus'
    %li
      = link_to 'Html', '#example-after-inline-html'

  .tab-content
    #example-after-inline-sass.tab-pane.active
      = preserve do
        -code("sass") do
          :plain
            .example-after-inline
              +cssowl-sprite-after-inline(-110px, -30px, 15px, 15px, url("sprite.png"), 5px, 1px, top)
    #example-after-inline-scss.tab-pane
      = preserve do
        -code("scss") do
          :plain
            .example-after-inline {
              @include cssowl-sprite-after-inline(-110px, -30px, 15px, 15px, url("sprite.png"), 5px, 1px, top);
            }
    #example-after-inline-less.tab-pane
      = preserve do
        -code("less") do
          :plain
            .example-after-inline {
              .cssowl-sprite-after-inline(-110px, -30px, 15px, 15px, url("sprite.png"), 5px, 1px, top);
            }
    #example-after-inline-stylus.tab-pane
      = preserve do
        -code("stylus") do
          :plain
            .example-after-inline
              cssowl-sprite-after-inline(-110px, -30px, 15px, 15px, url("sprite.png"), 5px, 1px, top)
    #example-after-inline-html.tab-pane
      = preserve do
        -code("html") do
          .example-after-inline Quia sint quis nam et quia ducimus consequatur cumque.