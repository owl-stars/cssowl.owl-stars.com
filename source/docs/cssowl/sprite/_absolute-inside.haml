%h2#cssowl-sprite-absolute-inside cssowl-sprite-absolute-inside <small>(x, y, width, height, src, valign, halign, xOffset, yOffset)</small>

%p
  :markdown
    Positions the element with `position: absolute;` inside the bounding box.

= preserve do
  -code("sass") do
    :plain
      /*
       * @param $x x position of the element
       * @param $y y position of the element
       * @param $width element width
       * @param $height element height
       * @param $src background image source
       * @param $valign (optional, default=top) vertical alignment top|middle|bottom
       * @param $halign (optional, default=left) horizontal alignment left|center|right
       * @param $xOffset (optional, default=0) x position offset
       * @param $yOffset (optional, default=0) y position offset
       */
      @mixin cssowl-sprite-absolute-inside($x, $y, $width, $height, $src, $valign:top, $halign:left, $xOffset:0, $yOffset:0)
        +cssowl-position-absolute-inside($width, $height, $valign, $halign, $xOffset, $yOffset)
        +cssowl-sprite-display($x, $y, $width, $height, $src)

.bs-docs-example

  .content
    .wrapper{:style => "position:relative;width:50px;height:50px;border:1px solid #59371f;"}
      .example-absolute-inside.top-center
      .example-absolute-inside.middle-right
      .example-absolute-inside.bottom-center
      .example-absolute-inside.middle-left

  %ul.nav.nav-tabs
    %li.active
      = link_to 'Sass', '#example-absolute-inside-sass'
    %li
      = link_to 'Scss', '#example-absolute-inside-scss'
    %li
      = link_to 'Less', '#example-absolute-inside-less'
    %li
      = link_to 'Stylus', '#example-absolute-inside-stylus'
    %li
      = link_to 'Html', '#example-absolute-inside-html'

  .tab-content
    #example-absolute-inside-sass.tab-pane.active
      = preserve do
        -code("sass") do
          :plain
            .example-absolute-inside
              &.top-center
                +cssowl-sprite-absolute-inside(-110px, -20px, 7px, 4px, url("sprite.png"), top, center)
              &.middle-right
                +cssowl-sprite-absolute-inside(-110px, -10px, 4px, 7px, url("sprite.png"), middle, right)
              &.bottom-center
                +cssowl-sprite-absolute-inside(-120px, -20px, 7px, 4px, url("sprite.png"), bottom, center)
              &.middle-left
                +cssowl-sprite-absolute-inside(-120px, -10px, 4px, 7px, url("sprite.png"), middle, left)
    #example-absolute-inside-scss.tab-pane
      = preserve do
        -code("scss") do
          :plain
            .example-absolute-inside {
              &.top-center
                @include cssowl-sprite-absolute-inside(-110px, -20px, 7px, 4px, url("sprite.png"), top, center);
              }
              &.middle-right {
                @include cssowl-sprite-absolute-inside(-110px, -10px, 4px, 7px, url("sprite.png"), middle, right);
              }
              &.bottom-center {
                @include cssowl-sprite-absolute-inside(-120px, -20px, 7px, 4px, url("sprite.png"), bottom, center);
              }
              &.middle-left {
                @include cssowl-sprite-absolute-inside(-120px, -10px, 4px, 7px, url("sprite.png"), middle, left);
              }
            }
    #example-absolute-inside-less.tab-pane
      = preserve do
        -code("less") do
          :plain
            .example-absolute-inside {
              &.top-center
                .cssowl-sprite-absolute-inside(-110px, -20px, 7px, 4px, url("sprite.png"), top, center);
              }
              &.middle-right {
                .cssowl-sprite-absolute-inside(-110px, -10px, 4px, 7px, url("sprite.png"), middle, right);
              }
              &.bottom-center {
                .cssowl-sprite-absolute-inside(-120px, -20px, 7px, 4px, url("sprite.png"), bottom, center);
              }
              &.middle-left {
                .cssowl-sprite-absolute-inside(-120px, -10px, 4px, 7px, url("sprite.png"), middle, left);
              }
            }
    #example-absolute-inside-stylus.tab-pane
      = preserve do
        -code("stylus") do
          :plain
            .example-absolute-inside
              &.top-center
                cssowl-sprite-absolute-inside(-110px, -20px, 7px, 4px, url("sprite.png"), top, center)
              &.middle-right
                cssowl-sprite-absolute-inside(-110px, -10px, 4px, 7px, url("sprite.png"), middle, right)
              &.bottom-center
                cssowl-sprite-absolute-inside(-120px, -20px, 7px, 4px, url("sprite.png"), bottom, center)
              &.middle-left
                cssowl-sprite-absolute-inside(-120px, -10px, 4px, 7px, url("sprite.png"), middle, left)
    #example-absolute-inside-html.tab-pane
      = preserve do
        -code("html") do
          .wrapper{:style => "position:relative;width:50px;height:50px;border:1px solid #59371f;"}
            .example-absolute-inside.top-center
            .example-absolute-inside.middle-right
            .example-absolute-inside.bottom-center
            .example-absolute-inside.middle-left