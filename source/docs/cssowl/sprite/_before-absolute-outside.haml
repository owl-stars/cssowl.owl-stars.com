%h3#cssowl-sprite-before-absolute-outside cssowl-sprite-before-absolute-outside <small>(x, y, width, height, src, valign, halign, xOffset, yOffset)</small>

:markdown
  Add a sprite element with the `:before` pseudo selector and place it with `position: absolute;` outside the bounding box.

= preserve do
  -code("sass") do
    :plain
      /*
       * @param $x x position of the element
       * @param $y y position of the element
       * @param $width element width
       * @param $height element height
       * @param $src background image source
       * @param $valign (optional, default=top) vertical alignment top|middle|bottom
       * @param $halign (optional, default=left) horizontal alignment left|center|right
       * @param $xOffset (optional, default=0) x position offset
       * @param $yOffset (optional, default=0) y position offset
       */
      @mixin cssowl-sprite-before-absolute-outside($x, $y, $width, $height, $src, $valign:top, $halign:left, $xOffset:0, $yOffset:0)
        position: relative
        &:before
          content: ""
          +cssowl-sprite-absolute-outside($x, $y, $width, $height, $src, $valign, $halign, $xOffset, $yOffset)

.bs-docs-example

  .content
    .example-before-absolute-outside{:style => "display:inline-block;border-top:1px solid #59371f;padding-top: 5px;"} Quia sint quis

    :sass
      @import "bower_components/cssowl/lib/sass/cssowl"
      .example-before-absolute-outside
        +cssowl-sprite-before-absolute-outside(-110px, -20px, 7px, 4px, url("/assets/img/sprite.png"), top, center)

  %ul.nav.nav-tabs
    %li.active.htmltab
      = link_to 'HTML', '#example-before-absolute-outside-html'
    %li
      = link_to 'SASS', '#example-before-absolute-outside-sass'
    %li
      = link_to 'SCSS', '#example-before-absolute-outside-scss'
    %li
      = link_to 'LESS', '#example-before-absolute-outside-less'
    %li
      = link_to 'Stylus', '#example-before-absolute-outside-styl'

  .tab-content
    #example-before-absolute-outside-html.tab-pane.active
      = preserve do
        -code("html") do
          .example-before-absolute-outside{:style => "display:inline-block;border-top:1px solid #59371f;padding-top: 5px;"} Quia sint quis
    #example-before-absolute-outside-sass.tab-pane
      = preserve do
        -code("sass") do
          :plain
            .example-before-absolute-outside
              +cssowl-sprite-before-absolute-outside(-110px, -20px, 7px, 4px, url("/assets/img/sprite.png"), top, center)
    #example-before-absolute-outside-scss.tab-pane
      = preserve do
        -code("scss") do
          :plain
            .example-before-absolute-outside {
              @include cssowl-sprite-before-absolute-outside(-110px, -20px, 7px, 4px, url("/assets/img/sprite.png"), top, center);
            }
    #example-before-absolute-outside-less.tab-pane
      = preserve do
        -code("scss") do
          :plain
            .example-before-absolute-outside {
              .cssowl-sprite-before-absolute-outside(-110px, -20px, 7px, 4px, url("/assets/img/sprite.png"), top, center);
            }
    #example-before-absolute-outside-styl.tab-pane
      = preserve do
        -code("sass") do
          :plain
            .example-before-absolute-outside
              cssowl-sprite-before-absolute-outside(-110px, -20px, 7px, 4px, url("/assets/img/sprite.png"), top, center)
