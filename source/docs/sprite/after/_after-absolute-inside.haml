%h2#cssowl-sprite-after-absolute-inside cssowl-sprite-after-absolute-inside <small>(x, y, width, height, src, valign, halign, xOffset, yOffset)</small>

%p
  :markdown
    Adds the element with the `:after` pseudo selector and displays it with `position: absolute;` inside the bounding box.

= preserve do
  -code("sass") do
    :plain
      /*
       * @param $y       x position of the element
       * @param $y       y position of the element
       * @param $width   element width
       * @param $height  element height
       * @param $src     background image source
       * @param $valign  (optional, default=top) vertical alignment top|middle|bottom
       * @param $halign  (optional, default=left) horizontal alignment left|center|right
       * @param $xOffset (optional, default=0) x position offset
       * @param $yOffset (optional, default=0) y position offset
       */
      @mixin cssowl-sprite-after-absolute-inside($x, $y, $width, $height, $src, $valign:top, $halign:left, $xOffset:0, $yOffset:0)
        position: relative
        &:after
          content: ""
          +cssowl-sprite-absolute-inside($x, $y, $width, $height, $src, $valign, $halign, $xOffset, $yOffset)

.bs-docs-example

  .content
    .example-after-absolute-inside{:style => "display:inline-block;border-top:1px solid #59371f;padding-top: 5px;"} Quia sint quis

  %ul.nav.nav-tabs
    %li.active
      = link_to 'Sass', '#example-after-absolute-inside-sass'
    %li
      = link_to 'Scss', '#example-after-absolute-inside-scss'
    %li
      = link_to 'Less', '#example-after-absolute-inside-less'
    %li
      = link_to 'Stylus', '#example-after-absolute-inside-stylus'
    %li
      = link_to 'Html', '#example-after-absolute-inside-html'

  .tab-content
    #example-after-absolute-inside-sass.tab-pane.active
      = preserve do
        -code("sass") do
          :plain
            .example-after-absolute-inside
              +cssowl-sprite-after-absolute-inside(-110px, -20px, 7px, 4px, url("sprite.png"), top, center)
    #example-after-absolute-inside-scss.tab-pane
      = preserve do
        -code("scss") do
          :plain
            .example-after-absolute-inside {
              @include cssowl-sprite-after-absolute-inside(-110px, -20px, 7px, 4px, url("sprite.png"), top, center);
            }
    #example-after-absolute-inside-less.tab-pane
      = preserve do
        -code("less") do
          :plain
            .example-after-absolute-inside {
              .cssowl-sprite-after-absolute-inside(-110px, -20px, 7px, 4px, url("sprite.png"), top, center);
            }
    #example-after-absolute-inside-stylus.tab-pane
      = preserve do
        -code("stylus") do
          :plain
            .example-after-absolute-inside
              cssowl-sprite-after-absolute-inside(-110px, -20px, 7px, 4px, url("sprite.png"), top, center)
    #example-after-absolute-inside-html.tab-pane
      = preserve do
        -code("html") do
          .example-after-absolute-inside{:style => "display:inline-block;border-top:1px solid #59371f;padding-top: 5px;"} Quia sint quis
