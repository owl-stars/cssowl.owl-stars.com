%h3#cssowl-coords-outside cssowl-coords-outside

:markdown
  Set an element's `top`, `right`, `bottom` and `left` properties to position it *outside* the bounding box.

.panel.panel-default
  .panel-heading
    = link_to 'Source', '#source-cssowl-coords-outside', data: {toggle: 'collapse'}

  #source-cssowl-coords-outside.panel-body.panel-collapse.collapse
    %ul.nav.nav-tabs
      %li.active
        = link_to 'SASS', '#source-cssowl-coords-outside-sass'
      %li
        = link_to 'SCSS', '#source-cssowl-coords-outside-scss'
      %li
        = link_to 'LESS', '#source-cssowl-coords-outside-less'
      %li
        = link_to 'Stylus', '#source-cssowl-coords-outside-styl'

    .tab-content
      #source-cssowl-coords-outside-sass.tab-pane.active
        = preserve do
          -code("sass") do
            :plain
              =cssowl-coords-outside($width, $height, $valign:top, $halign:left, $xOffset:0, $yOffset:0)
                @if $valign == top
                  top: round($yOffset - $height)
                @else if $valign == middle
                  top: 50%
                  margin-top: $yOffset - round($height / 2)
                @else if $valign == bottom
                  bottom: round($yOffset - $height)
                @if $halign == left
                  left: round($xOffset - $width)
                @else if $halign == center
                  left: 50%
                  margin-left: $xOffset - round($width / 2)
                @else if $halign == right
                  right: round($xOffset - $width)
      #source-cssowl-coords-outside-scss.tab-pane
        = preserve do
          -code("scss") do
            :plain
              @mixin cssowl-coords-outside($width, $height, $valign:top, $halign:left, $xOffset:0, $yOffset:0) {
                @if $valign == top {
                  top: round($yOffset - $height);
                }
                @else if $valign == middle {
                  top: 50%;
                  margin-top: $yOffset - round($height / 2);
                }
                @else if $valign == bottom {
                  bottom: round($yOffset - $height);
                }
                @if $halign == left {
                  left: round($xOffset - $width);
                }
                @else if $halign == center {
                  left: 50%;
                  margin-left: $xOffset - round($width / 2);
                }
                @else if $halign == right {
                  right: round($xOffset - $width);
                }
              }
      #source-cssowl-coords-outside-less.tab-pane
        = preserve do
          -code("scss") do
            :plain
              .cssowl-coords-outside(@width, @height, @valign:top, @halign:left, @xOffset:0, @yOffset:0) when (@valign = top) {
                top: round(@yOffset - @height);
              }
              .cssowl-coords-outside(@width, @height, @valign:top, @halign:left, @xOffset:0, @yOffset:0) when (@valign = middle) {
                top: 50%;
                margin-top: @yOffset - round(@height / 2);
              }
              .cssowl-coords-outside(@width, @height, @valign:top, @halign:left, @xOffset:0, @yOffset:0) when (@valign = bottom) {
                bottom: round(@yOffset - @height);
              }
              .cssowl-coords-outside(@width, @height, @valign:top, @halign:left, @xOffset:0, @yOffset:0) when (@halign = left) {
                left: round(@xOffset - @width);
              }
              .cssowl-coords-outside(@width, @height, @valign:top, @halign:left, @xOffset:0, @yOffset:0) when (@halign = center) {
                left: 50%;
                margin-left: @xOffset - round(@width / 2);
              }
              .cssowl-coords-outside(@width, @height, @valign:top, @halign:left, @xOffset:0, @yOffset:0) when (@halign = right) {
                right: round(@xOffset - @width);
              }
              .cssowl-coords-outside(@width, @height, @valign:top, @halign:left, @xOffset:0, @yOffset:0) {
                // see conditionals
              }
      #source-cssowl-coords-outside-styl.tab-pane
        = preserve do
          -code("sass") do
            :plain
              cssowl-coords-outside(width, height, valign=top, halign=left, xOffset=0, yOffset=0)
                if valign == top
                  top round(yOffset - height)
                else if valign == middle
                  top 50%
                  margin-top yOffset - round(height / 2)
                else if valign == bottom
                  bottom round(yOffset - height)
                if halign == left
                  left round(xOffset - width)
                else if halign == center
                  left 50%
                  margin-left xOffset - round(width / 2)
                else if halign == right
                  right round(xOffset - width)


.panel.panel-default
  .panel-heading
    = link_to 'Examples', '#examples-cssowl-coords-outside', data: {toggle: 'collapse'}

  #examples-cssowl-coords-outside.panel-body.panel-collapse.collapse.in.bs-docs-example

    .content
      .wrapper{:style => "position:relative;width:50px;height:50px;border:1px solid #59371f;"}
        .example-coords-outside.top-center
        .example-coords-outside.middle-right
        .example-coords-outside.bottom-center
        .example-coords-outside.middle-left

      :sass
        $sprite-sheet-image: url("/assets/img/site/sprite.png")
        $sprite-sheet-image-2x: url("/assets/img/site/sprite@2x.png")
        @import "site/sprite"
        @import "bower_components/cssowl/lib/sass/cssowl"
        .example-coords-outside
          width: 10px
          height: 10px
          position: absolute
          background: #59371f
          &.top-center
            +cssowl-coords-outside(10px, 10px, top, center)
          &.middle-right
            +cssowl-coords-outside(10px, 10px, middle, right)
          &.bottom-center
            +cssowl-coords-outside(10px, 10px, bottom, center)
          &.middle-left
            +cssowl-coords-outside(10px, 10px, middle, left)

    %ul.nav.nav-tabs
      %li.active
        = link_to 'HTML', '#example-cssowl-coords-outside-html'
      %li.htmltab
        = link_to 'CSS', '#example-cssowl-coords-outside-css'
      %li
        = link_to 'SASS', '#example-cssowl-coords-outside-sass'
      %li
        = link_to 'SCSS', '#example-cssowl-coords-outside-scss'
      %li
        = link_to 'LESS', '#example-cssowl-coords-outside-less'
      %li
        = link_to 'Stylus', '#example-cssowl-coords-outside-styl'

    .tab-content
      #example-cssowl-coords-outside-html.tab-pane.active
        = preserve do
          -code("html") do
            .wrapper{:style => "position:relative;width:50px;height:50px;border:1px solid #59371f;"}
              .example-coords-outside.top-center
              .example-coords-outside.middle-right
              .example-coords-outside.bottom-center
              .example-coords-outside.middle-left
      #example-cssowl-coords-outside-css.tab-pane
        = preserve do
          -code("css") do
            :sass
              @import "bower_components/cssowl/lib/sass/cssowl"
              @import "source/assets/css/site/sprite"
              .example-coords-outside
                width: 10px
                height: 10px
                position: absolute
                background: #59371f
                &.top-center
                  +cssowl-coords-outside(10px, 10px, top, center)
                &.middle-right
                  +cssowl-coords-outside(10px, 10px, middle, right)
                &.bottom-center
                  +cssowl-coords-outside(10px, 10px, bottom, center)
                &.middle-left
                  +cssowl-coords-outside(10px, 10px, middle, left)
      #example-cssowl-coords-outside-sass.tab-pane
        = preserve do
          -code("sass") do
            :plain
              .example-coords-outside
                width: 10px
                height: 10px
                position: absolute
                background: #59371f
                &.top-center
                  +cssowl-coords-outside(10px, 10px, top, center)
                &.middle-right
                  +cssowl-coords-outside(10px, 10px, middle, right)
                &.bottom-center
                  +cssowl-coords-outside(10px, 10px, bottom, center)
                &.middle-left
                  +cssowl-coords-outside(10px, 10px, middle, left)
      #example-cssowl-coords-outside-scss.tab-pane
        = preserve do
          -code("scss") do
            :plain
              .example-coords-outside {
                width: 10px;
                height: 10px;
                position: absolute;
                background: #59371f;
                &.top-center {
                  @include cssowl-coords-outside(10px, 10px, top, center);
                }
                &.middle-right {
                  @include cssowl-coords-outside(10px, 10px, middle, right);
                }
                &.bottom-center {
                  @include cssowl-coords-outside(10px, 10px, bottom, center);
                }
                &.middle-left {
                  @include cssowl-coords-outside(10px, 10px, middle, left);
                }
              }
      #example-cssowl-coords-outside-less.tab-pane
        = preserve do
          -code("scss") do
            :plain
              .example-coords-outside {
                width: 10px;
                height: 10px;
                position: absolute;
                background: #59371f;
                &.top-center {
                  .cssowl-coords-outside(10px, 10px, top, center);
                }
                &.middle-right {
                  .cssowl-coords-outside(10px, 10px, middle, right);
                }
                &.bottom-center {
                  .cssowl-coords-outside(10px, 10px, bottom, center);
                }
                &.middle-left {
                  .cssowl-coords-outside(10px, 10px, middle, left);
                }
              }
      #example-cssowl-coords-outside-styl.tab-pane
        = preserve do
          -code("sass") do
            :plain
              .example-coords-outside
                width 10px
                height 10px
                position absolute
                background #59371f
                &.top-center
                  cssowl-coords-outside(10px, 10px, top, center)
                &.middle-right
                  cssowl-coords-outside(10px, 10px, middle, right)
                &.bottom-center
                  cssowl-coords-outside(10px, 10px, bottom, center)
                &.middle-left
                  cssowl-coords-outside(10px, 10px, middle, left)
